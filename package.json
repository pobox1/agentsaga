{
  "name": "agentsaga",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @agentsaga/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "test:contracts": "./.tools/foundry/forge test -vvv",
    "typecheck": "pnpm -r typecheck"
  }
}

