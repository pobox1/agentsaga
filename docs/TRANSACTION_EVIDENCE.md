# Transaction evidence

Evidence must be copied from Arc RPC/ArcScan after confirmation and include
network, chain ID, transaction hash, block, contract address, and the exact
workflow/receipt event. Local Anvil and deterministic x402 fixtures must be
labelled `local` or `simulated`; they are not public testnet proof.

No public deployment or workflow transaction is recorded in this checkout yet.
